{
	"$schema": "https://apify.com/schemas/v1/actor.ide.json",
	"actorSpecification": 1,
	"name": "normal-test-actor",
	"title": "Apify normal test actor",
	"description": "Actor used for MCP server testing. Runs only in normal actor mode: reads two integers from input, computes their sum, and pushes the result to the default dataset.",
	"version": "0.0",
	"buildTag": "latest",
	"usesStandbyMode": false,
	"meta": {
		"templateId": "ts-mcp-empty",
		"generatedBy": "Claude Code with Claude Opus 4.7 (1M context)"
	},
	"input": {
		"title": "Actor input schema",
		"description": "Input for normal actor runs.",
		"type": "object",
		"schemaVersion": 1,
		"properties": {
			"firstNumber": {
				"title": "First number",
				"type": "integer",
				"description": "First addend.",
				"default": 1,
				"prefill": 1
			},
			"secondNumber": {
				"title": "Second number",
				"type": "integer",
				"description": "Second addend.",
				"default": 2,
				"prefill": 2
			},
			"waitSeconds": {
				"title": "Wait seconds",
				"type": "integer",
				"description": "Number of seconds to wait before computing the sum.",
				"default": 0,
				"prefill": 0,
				"minimum": 0
			},
			"includeBookFixture": {
				"title": "Include book fixture",
				"type": "boolean",
				"description": "If true, push the Apify book fixture (3 nested items) to the default dataset instead of the sum, and write RESULT + STATS records to the default key-value store.",
				"default": false
			}
		},
		"required": ["firstNumber", "secondNumber"]
	},
	"storages": {
		"dataset": "./dataset_schema.json",
		"keyValueStore": "./key_value_store_schema.json"
	},
	"dockerfile": "../Dockerfile"
}
