{
	"$schema": "https://apify.com/schemas/v1/actor.ide.json",
	"actorSpecification": 1,
	"name": "mcp-server-test-actor",
	"title": "Apify MCP server test actor",
	"description": "Actor used for MCP server testing. Supports two execution modes: as an MCP server using the Apify standby mode and a normal actor mode",
	"version": "0.0",
	"buildTag": "latest",
	"usesStandbyMode": true,
	"meta": {
		"templateId": "ts-mcp-empty",
		"generatedBy": "Claude Code with Claude Opus 4.7 (1M context)"
	},
	"input": {
		"title": "Actor input schema",
		"description": "Input for normal (non-standby) actor runs. Ignored in standby/MCP server mode, where input is received via HTTP.",
		"type": "object",
		"schemaVersion": 1,
		"properties": {
			"firstNumber": {
				"title": "First number",
				"type": "integer",
				"description": "First addend. Used only in normal-run mode.",
				"default": 1,
				"prefill": 1
			},
			"secondNumber": {
				"title": "Second number",
				"type": "integer",
				"description": "Second addend. Used only in normal-run mode.",
				"default": 2,
				"prefill": 2
			}
		},
		"required": ["firstNumber", "secondNumber"]
	},
	"dockerfile": "../Dockerfile",
	"webServerMcpPath": "/mcp"
}
